<UserControl x:Class="Errorreport_Assignment.MVVM.Views.ErrorReportDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Errorreport_Assignment.MVVM.Views" 
             xmlns:viewmodels="clr-namespace:Errorreport_Assignment.MVVM.ViewModels"
             xmlns:model="clr-namespace:Errorreport_Assignment.MVVM.Models"
             d:DataContext="{d:DesignInstance Type=viewmodels:ErrorReportDetailsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <StackPanel>
            <TextBlock 
                Text="Detaljerad information:"
                FontSize="16" 
                Margin="0 20 0 0" 
                FontWeight="DemiBold" 
                Foreground="#554c4b"/>

            <TextBlock 
                Margin="5 5 0 10" 
                Foreground="#554c4b">
                <Run Text="Markera ärendet du vill veta mer om, i ärendelistan, som du hittar när du trycker på knappen "/>
                <Run 
                    FontWeight="Bold"
                    Text="&quot;Alla ärenden&quot;"/>
                <Run Text="!"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
